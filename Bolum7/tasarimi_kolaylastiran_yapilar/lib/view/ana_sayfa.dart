import 'package:flutter/material.dart';

class AnaSafya extends StatelessWidget {
  const AnaSafya({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("ANA SAYFA"),
    );
  }
}
