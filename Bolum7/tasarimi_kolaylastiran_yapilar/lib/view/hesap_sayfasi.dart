import 'package:flutter/material.dart';

class HesapPage extends StatelessWidget {
  const HesapPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hesap page"),
    );
  }
}
